# SPDX-License-Identifier: MIT
---
bugfixes:
  - >-
    ha_database - an empty ``ha_database_pool`` selects the first diskful
    pool on the combined nodes, as documented, instead of failing the
    conversion.
minor_changes:
  - >-
    ha_database - check that an explicit ``ha_database_pool`` is a diskful
    pool on every combined node before the conversion starts.
