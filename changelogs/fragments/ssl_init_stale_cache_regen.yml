# SPDX-License-Identifier: MIT
---
bugfixes:
  - >-
    ssl_init - regenerate a cached node certificate whose SAN no longer
    matches the inventory, and rebuild the keystore and truststores when the
    pushed certificate, key, or CA changes. Stale material failed TLS
    hostname verification and skipped the service restart.
  - >-
    ssl_init - retry the controller status query on transient failures and
    do not double-count controllers when the role runs twice in one play.
