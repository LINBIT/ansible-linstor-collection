breaking_changes:
  - ha_database - the HA database resource name is now fixed to linstor_db
    and the ha_database_res role variable is removed. The name is shared
    with the LINSTOR user guide and the linstor-controller-ha-setup script,
    and overriding it would break the cross-tool conversion detection.
