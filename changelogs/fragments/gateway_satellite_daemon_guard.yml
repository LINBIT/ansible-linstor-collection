# SPDX-License-Identifier: MIT
---
bugfixes:
  - gateway_satellite - skip token authentication configuration on nodes
    without the ``linstor-gateway`` service instead of failing the play by
    restarting a nonexistent unit.
