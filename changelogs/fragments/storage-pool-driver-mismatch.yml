# SPDX-License-Identifier: MIT
---
bugfixes:
  - >-
    storage_pool - fail when the pool already exists with a different driver or
    backing pool instead of silently reporting ``ok``. LINSTOR cannot change
    either on an existing pool, so the message tells the user to delete the pool
    with ``state=absent`` and create it again.
