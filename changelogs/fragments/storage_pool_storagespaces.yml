# SPDX-License-Identifier: MIT
---
minor_changes:
  - >-
    storage_pool - create Windows Storage Spaces pools on LINBIT SDS for
    Windows satellites with the ``storagespaces`` and ``storagespaces_thin``
    types and the ``wss`` key. The role builds the pool from
    ``physical_devices`` and refuses boot and system disks.
