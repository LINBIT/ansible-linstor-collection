# SPDX-License-Identifier: MIT
---
bugfixes:
  - auth_init - default ``linstor_api_delegate`` to ``localhost`` like the other
    LINSTOR API roles, so the role runs standalone without the inventory
    defining it.
