# SPDX-License-Identifier: MIT
---
minor_changes:
  - >-
    gateway_satellite - install SCST with the
    ``linbit.drbd_reactor.scst_install`` role when
    ``gateway_satellite_scst`` is ``true``. By default it builds the latest
    SCST release.
breaking_changes:
  - >-
    gateway_satellite - ``gateway_satellite_scst_version`` is removed. Set
    ``scst_install_version`` to pin the SCST build reference.
