# SPDX-License-Identifier: MIT
---
bugfixes:
  - >-
    ssl_init - create ``/etc/linstor/ssl`` at mode ``0755`` so the non-root
    controller service user of LINSTOR 1.34.0 can read its keystore. The
    private key keeps mode ``0600``.
