# SPDX-License-Identifier: MIT
---
minor_changes:
  - >
    cluster_membership - pre-flight that the control-node's
    ``~/.config/linstor/linstor-client.conf`` actually reaches the
    LINSTOR controller and fail with an actionable message pointing
    at ``client_install`` to refresh stale local config, instead of
    letting the first module call fail with a generic connection
    error.
