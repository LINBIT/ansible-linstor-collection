# SPDX-License-Identifier: MIT
---
- name: Open firewalld ports for LINSTOR Gateway
  ansible.posix.firewalld:
    port: "{{ item.port }}/{{ item.proto }}"
    zone: "{{ item.zone | default(omit, true) }}"
    permanent: true
    immediate: true
    state: enabled
  loop: "{{ gateway_install_firewall_ports }}"
