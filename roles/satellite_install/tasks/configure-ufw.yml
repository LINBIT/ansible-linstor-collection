# SPDX-License-Identifier: MIT
---
- name: Open UFW ports for LINSTOR satellite
  community.general.ufw:
    rule: allow
    port: "{{ item.port | regex_replace('-', ':') }}"
    proto: "{{ item.proto }}"
  loop: "{{ satellite_install_firewall_ports }}"
