# SPDX-License-Identifier: MIT
---
- name: Remove version locks for LINSTOR satellite packages (Zypper)
  ansible.builtin.command:
    cmd: "zypper removelock {{ item }}"
  loop:
    - linstor-satellite
    - linstor-common
  changed_when: false
  failed_when: false
