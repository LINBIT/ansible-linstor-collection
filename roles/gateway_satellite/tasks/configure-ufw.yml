# SPDX-License-Identifier: MIT
---
- name: Open UFW ports for LINSTOR Gateway satellite services
  community.general.ufw:
    rule: allow
    port: "{{ item.port | regex_replace('-', ':') }}"
    proto: "{{ item.proto }}"
  loop: "{{ gateway_satellite_firewall_ports }}"
