# SPDX-License-Identifier: MIT
---
_storage_pool_has_pools: "{{ _my_pools | length > 0 }}"
_storage_pool_zfs_path: >-
  {%- if ansible_facts['os_family'] | lower == 'redhat' -%}
    /sbin/zfs
  {%- else -%}
    /usr/sbin/zfs
  {%- endif -%}
